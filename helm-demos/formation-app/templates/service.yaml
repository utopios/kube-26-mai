apiVersion: v1
kind: Service
metadata:
  name: {{ include "formation-app.fullname" . }}
  labels:
    {{- include "formation-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "formation-app.selectorLabels" . | nindent 4 }}
